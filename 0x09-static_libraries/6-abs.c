#include "main.h"

/**
 * _abs - this function computes an absolute volume of an integer
 *@a: parameter determinant
 * Return: set to a
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);


}
