#include "main.h"

/**
 * _abs - this function computes an absolute volume of an integer
 * @n: parameter determinant
 * Return: set to n
 */
int _abs(int n)
{
	if (n < 0)
		n *= -1;

	return (n);

}
