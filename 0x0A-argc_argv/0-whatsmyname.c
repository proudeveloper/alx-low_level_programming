#include "stdio.h"

/**
 * main - Program that prints its name
 * @argc: int
 * @argv: char*
 * Return: Always 0
 */
int main(__attribute__((unused)) int argc, char **argv)
{
	puts(argv[0]);

	return (0);
}
